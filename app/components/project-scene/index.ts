// Importing this module pulls in three.js and R3F, so callers load it with
// React.lazy rather than importing it into the page bundle.
export { ProjectScene } from './project-scene';
export type { ProjectSceneProps } from './project-scene';
